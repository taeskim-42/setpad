# Play 업로드용 서비스 계정. attamama-android 와 같은 것을 쓴다.
json_key_file(ENV.fetch("SUPPLY_JSON_KEY_PATH",
                        File.expand_path("~/.playconsole/darak-play-service-account.json")))
package_name("com.taeskim.setpad")
